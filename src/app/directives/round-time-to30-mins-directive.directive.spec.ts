import { RoundTimeTo30MinsDirectiveDirective } from './round-time-to30-mins-directive.directive';

describe('RoundTimeTo30MinsDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RoundTimeTo30MinsDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
